# Local dev
VITE_API_URL=http://localhost:3001

# Production - set in Netlify → Site settings → Environment variables → Add
# VITE_API_URL=https://clicks-task.onrender.com
# Then redeploy (env vars are read at build time)
